# Security

Never commit Firebase service-account JSON, FCM server credentials, private API URLs, real device tokens, user data, or company code.

This demo uses a mock push provider specifically so the architecture can be reviewed without exposing credentials.
